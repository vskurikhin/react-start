CREATE SCHEMA IF NOT EXISTS front AUTHORIZATION DBA;

CREATE TABLE IF NOT EXISTS front."USER" (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY (start with 1),
  name VARCHAR(255),
  password VARCHAR(255),
  role varchar(255),
  PRIMARY KEY (id));
